# Pré-requisito: copie este arquivo para .env antes de rodar o app (cp .env.example .env).
# Preencha SUPABASE_ANON_KEY em Project Settings → API no Supabase.
# Nunca commite .env (está no .gitignore).

SUPABASE_URL=https://mkyftoqllkvemzyxcnes.supabase.co
SUPABASE_ANON_KEY=sua_anon_key_aqui
